{
  "framework": "nextjs",
  "buildCommand": "cd frontend && npx prisma generate && next build",
  "outputDirectory": "frontend/.next",
  "installCommand": "cd frontend && npm install",
  "rootDirectory": "frontend",
  "env": {
    "DATABASE_URL": "@database_url"
  }
}
